{
  "framework": "nextjs",
  "buildCommand": "npm run build:next",
  "outputDirectory": ".next",
  "installCommand": "npm install && npx prisma generate",
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  }
}